# Полная инструкция: Загрузка на GitHub и деплой на Vercel

## 📋 Что нужно сделать

1. Установить Git
2. Загрузить все файлы на GitHub
3. Подключить GitHub к Vercel
4. Получить публичную ссылку

---

## Шаг 1: Установка Git

### Windows:
1. Скачайте Git: https://git-scm.com/download/win
2. Установите (все настройки по умолчанию)
3. Откройте Git Bash (или используйте терминал в VS Code)

### Mac:
1. Git уже установлен (проверьте: `git --version` в терминале)
2. Если нет: установите через Xcode Command Line Tools

### Проверка:
Откройте терминал и выполните:
```bash
git --version
```
Должно показать версию Git (например, `git version 2.40.0`)

---

## Шаг 2: Регистрация на GitHub

1. Зайдите на https://github.com
2. Нажмите "Sign up" (Регистрация)
3. Заполните форму:
   - Username (имя пользователя)
   - Email
   - Password (пароль)
4. Подтвердите email
5. Войдите в аккаунт

---

## Шаг 3: Создание репозитория на GitHub

1. На GitHub нажмите **"+"** в правом верхнем углу → **"New repository"**
2. Заполните:
   - **Repository name:** `KikoGame` (или любое имя)
   - **Description:** (опционально) "AI Knowledge Assessment Game"
   - **Public** или **Private** (выберите Public для бесплатного деплоя)
3. **НЕ** ставьте галочки на "Add README", "Add .gitignore", "Choose a license"
4. Нажмите **"Create repository"**

---

## Шаг 4: Загрузка файлов на GitHub

### Вариант A: Через терминал (рекомендуется)

1. **Откройте терминал** в папке проекта:
   ```bash
   cd "/Users/ivanfrolovich/Desktop/Февраль/KiKoGame"
   ```

2. **Проверьте статус Git:**
   ```bash
   git status
   ```
   
   Если видите "On branch main" - Git уже инициализирован, переходите к шагу 5.
   
   Если видите "not a git repository" - выполните:
   ```bash
   git init
   git branch -M main
   ```

3. **Добавьте все файлы:**
   ```bash
   git add .
   ```
   Это добавит ВСЕ файлы из папки (код, картинки, звуки)

4. **Создайте коммит:**
   ```bash
   git commit -m "Initial commit - KikoGame web version"
   ```

5. **Подключите к GitHub:**
   ```bash
   git remote add origin https://github.com/ВАШ_USERNAME/KikoGame.git
   ```
   Замените `ВАШ_USERNAME` на ваш username с GitHub!

6. **Загрузите на GitHub:**
   ```bash
   git push -u origin main
   ```
   
   Вас попросят ввести логин и пароль GitHub (или токен доступа)

### Вариант B: Через GitHub Desktop (проще для начинающих)

1. Скачайте GitHub Desktop: https://desktop.github.com
2. Установите и войдите в свой GitHub аккаунт
3. File → Add Local Repository
4. Выберите папку проекта: `/Users/ivanfrolovich/Desktop/Февраль/KiKoGame`
5. Нажмите "Publish repository"
6. Выберите имя репозитория и нажмите "Publish"

---

## Шаг 5: Подключение к Vercel

1. **Зайдите на https://vercel.com**
2. Нажмите **"Sign Up"** или **"Log In"**
3. Выберите **"Continue with GitHub"** (войти через GitHub)
4. Разрешите Vercel доступ к вашему GitHub аккаунту

---

## Шаг 6: Создание проекта в Vercel

1. В Vercel нажмите **"Add New Project"** (или **"New Project"**)
2. Вы увидите список ваших GitHub репозиториев
3. Найдите и выберите **"KikoGame"** (или как вы назвали репозиторий)
4. Нажмите **"Import"**

---

## Шаг 7: Настройка проекта в Vercel

После импорта вы увидите форму настройки:

### Обязательные настройки:

1. **Root Directory:**
   - Нажмите **"Edit"** рядом с полем
   - Введите: `web`
   - Это говорит Vercel, что файлы игры находятся в папке `web/`

2. **Framework Preset:**
   - Выберите **"Other"** (или оставьте по умолчанию)

3. **Build Command:**
   - Оставьте **пустым** (для статического сайта не нужна сборка)

4. **Output Directory:**
   - Оставьте **`.`** (точка) или **пустым**

5. **Project Name:**
   - Можете оставить `kiko-game` или изменить

### Необязательные настройки:

- **Environment Variables:** не нужны
- **Build and Output Settings:** не нужны

---

## Шаг 8: Деплой

1. Нажмите кнопку **"Deploy"** внизу формы
2. Дождитесь завершения деплоя (1-2 минуты)
3. Вы увидите прогресс:
   - Provisioning ✓
   - Building ✓
   - Deploying ✓

---

## Шаг 9: Получение ссылки

После успешного деплоя:

1. Вы увидите страницу с сообщением **"Congratulations!"**
2. Ваша ссылка будет вверху: `https://kiko-game.vercel.app` (или другое имя)
3. **Скопируйте эту ссылку** - это публичная ссылка для сотрудников!

---

## 📁 Какие файлы загружаются

### Все файлы из папки проекта:

```
KiKoGame/
├── web/                    ← ВСЯ папка web (JavaScript версия)
│   ├── index.html
│   ├── *.js (все JS файлы)
│   ├── style.css
│   └── PICS/              ← ВСЕ изображения и звуки
│       ├── Background/
│       ├── Departaments/
│       ├── Enemy/
│       ├── Music/
│       ├── Player_left/
│       ├── Player_right/
│       ├── Rules/
│       └── Stats/
├── *.py                   ← Python файлы (оригинальная версия)
├── README.md
├── requirements.txt
├── .gitignore
└── vercel.json
```

**Важно:** Загружаются **ВСЕ** файлы, включая:
- ✅ Весь код (Python и JavaScript)
- ✅ Все изображения (PICS/)
- ✅ Все звуки (PICS/Music/)
- ✅ Конфигурационные файлы

---

## 🔄 Обновление проекта

Если вы изменили файлы:

1. **В терминале:**
   ```bash
   cd "/Users/ivanfrolovich/Desktop/Февраль/KiKoGame"
   git add .
   git commit -m "Описание изменений"
   git push
   ```

2. **Vercel автоматически передеплоит** проект (обычно через 1-2 минуты)

---

## ❓ Частые вопросы

### Q: Нужно ли загружать Python файлы?
**A:** Да, можно загрузить все. Vercel использует только папку `web/` благодаря настройке Root Directory.

### Q: Что если я забыл пароль GitHub?
**A:** Используйте "Forgot password" на странице входа GitHub.

### Q: Можно ли изменить имя проекта в Vercel?
**A:** Да, в настройках проекта можно изменить имя и получить новую ссылку.

### Q: Сколько стоит Vercel?
**A:** Бесплатный план достаточен для вашего проекта. Ограничения:
- Неограниченное количество проектов
- Автоматические деплои из GitHub
- Публичные ссылки

### Q: Что если деплой не работает?
**A:** Проверьте:
1. Root Directory установлен как `web`
2. Все файлы загружены на GitHub
3. В консоли Vercel нет ошибок

---

## 📝 Пошаговая шпаргалка

```
1. Установить Git
2. Зарегистрироваться на GitHub
3. Создать репозиторий на GitHub
4. В терминале:
   cd "/путь/к/проекту"
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/USERNAME/KikoGame.git
   git push -u origin main
5. Зарегистрироваться на Vercel (через GitHub)
6. Import проект → выбрать KikoGame
7. Root Directory: web
8. Deploy
9. Скопировать ссылку
```

---

## 🎉 Готово!

После выполнения всех шагов у вас будет:
- ✅ Проект на GitHub
- ✅ Публичная ссылка на Vercel
- ✅ Игра работает в браузере

**Ссылку можно отправлять сотрудникам!**

